<div class="uk-container uk-container-center uk-margin-top uk-margin-large-bottom">
    <nav class="uk-navbar">
        <ul class="uk-navbar-nav">
            <li class="">{{#link-to 'application' }}Home{{/link-to}}</li>
            {{#each model as |category|}}
                <li class="">{{#link-to 'products' category.id}}{{category.name}}{{/link-to}}</li>
            {{/each}}
        </ul>
        <div>{{cart-resume}}</div>
    </nav>
    <div data-uk-grid>

        {{outlet}}
    </div>
</div>